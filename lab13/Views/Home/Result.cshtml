@model QuizGenerator
@{
    ViewData["Title"] = "Hello Quiz";
}

<h1>Quiz results</h1>

@if (!@Model.isEmpty())
{
    <p>Right answers @Model.rightAnswers of @Model.problems.Count</p>
}

<ul class="list">
    @if (@Model.isEmpty()) {
        <li class="bold">No data</li>
    }
    else {
        int counter = 0;

        foreach (var expression in @Model.problems)
        {
            counter++;

            <li>@counter. @expression.firstValue @expression.operation @expression.secondValue = @expression.userAnswer</li>
        }
    }
</ul>